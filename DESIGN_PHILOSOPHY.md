# 记忆库设计理念

## 一句话

**SQLite 是权威存储，Markdown 是无损投影。**

数据在 SQLite 中活着——快、原子、可查询、可索引。随时可以整体导出成一份可读的 Markdown 目录树。任何符合投影结构的 Markdown 目录也可以一次性导入，导入完再导出的结果与导入前位对位一致。

> **术语约定：** 存储层概念是**组（group）**——一个主题容器，把相似条目归拢在一起（`groups` 表一行），导出时才对应一个 `category/slug.md` 文件。冷存储按创建时间分成**批次（batch）**（`cold_batches` 表一行），不是主题组。文档中"md 文件"均指导出/导入的磁盘产物。

## 为什么用 SQLite 作为权威存储

记忆系统的语义要求——组级 dirty、条目级操作、冷存储时间队列、LLM 主导语义整理、Python 只做机械工作——天生和"每次写都要读写整个 md 文件、重建全仓 FTS 索引"这种做法矛盾。仓库大到一定程度后，机械代价随规模线性甚至平方级增长。

SQLite 从根子上解决了这个问题：

| 操作 | 实现代价 |
|---|---|
| 写入一条记忆 | 单行 INSERT + FTS 索引同步 |
| 添加评论 | UPDATE 一行 JSON 列 |
| 机械维护（冷迁移） | SQL SELECT + DELETE + INSERT |
| 并发读写 | 事务 + WAL 自然序列化 |

用户能感知的只有一件事：**LLM 工具语义等价**。机械层选更省的实现即可。

## 为什么保留 Markdown 投影

三个动机：

1. **数据可搬走。** 用户任何时候可以导出一份可以用 vim 打开的目录树。数据主权在用户手里，不因存储引擎而被锁。
2. **可审计。** 数据出问题时，导出成 md diff 一眼能看到差异；纯二进制存储无此便利。
3. **可迁入。** 已有的 Markdown 记忆库（任意来源）只要符合投影结构就能一键导入。

投影是**只读的镜像**，不是双向真源。写入路径始终是 SQLite，不存在"改了 md 会自动同步"的机制。

## 数据模型：条目为单位，组为容器

记忆的原子单位是**条目**（entry）。条目是 LLM 能读、能写、能引用的最小信息单元——一段知识、一个命令、一条经验。**一条目应该是自包含的：单独读出来时能给读者完整答案，不强依赖同一组的其他条目。** 如果一个组里的多个条目必须互相配合才能构成完整知识，它们应该被合并成一条（内部用 markdown 分节）。

**组**只是把相似条目归拢到一起的**容器**。这个"相似条目放在一起"的关联性是整个系统的隐式关键属性（见下文"隐式关联性"）。

这个观察决定了几个设计选择：

- **dirty 是组级**，不是条目级。任何一次写入都会把整个组标脏，因为新条目可能和组里的老条目重叠、需要 LLM 判断要不要合并。
- **搜索单位是条目**（FTS5 索引在条目级别），但**修改单位是组**（重写时一次替换整个组的条目列表）。两个粒度错开是有意的：搜索要精细，整理要一次性看到相关上下文。
- **冷存储没有组级归属**。条目搬到冷存储后，按**冷批次的创建时间**顺序追加，不再按主题归类。冷存储是时间队列，不是分类库。

## 核心不变量

以下四条是所有设计决策的锚点，任何一条被打破都是回归。

### 1. Python 检测，LLM 决策，重写持久化

机械维护只做检测和搬运（冷迁移、检测超限），**从不清 dirty**。LLM 读到 dirty 组后自行整理，唯一能把 dirty 归零的路径是重写操作——传入整理后的完整条目列表。

### 2. Python 从不物理删除活跃条目

两个理由：一，活跃条目的删除意味着数据永久丢失，LLM 应该有机会把值得留的条目挪到冷存储或合并到别的组；二，测试可以断言机械维护前后活跃条目数不减少。唯一物理删除路径是冷存储超限时按创建时间（`cold_batches.created` 字段）删最老的冷批次。

### 3. 冷存储是"追加到最新批次"的队列

不是按驱逐日期分组，而是全部追加到当前最新的冷批次里。实现上总是复用最新批次（`get_or_create_cold_batch_for_today`）；"批次写满才新建"是预留的 rollover 机制（`rollover_cold_batch`），当前尚未接入自动流程。因此：
- 冷批次文件名 = **批次创建时间**，不是条目驱逐时间
- 一个冷批次里可能混了不同日期驱逐的条目
- 冷批次上限触发时按创建时间（`cold_batches.created`）删最老者，不解析文件名

### 4. Cold recall 是只读的

读一份冷条目内容，**不改**冷存储。用户如果要"救回"这个条目，应该在读到内容后自己用写入工具写进活跃库，允许冷/活双份共存。避免了"救回操作要在两个位置维护状态"的复杂性。

## 工具形态

工具按"日常读写"和"维护整理"两类分层：

| 工具 | 责任 |
|---|---|
| `note_search` | FTS5 搜索，只搜活跃条目，不含冷 |
| `note_write` | 追加条目，新条目自动置 dirty |
| `note_read` | 读一个活跃组——传 `entry_header` 拿单条，不传拿精简概要（headers 列表，无 content） |
| `note_read_group` | 一次拉整个组的所有条目 + 评论，**仅维护流程使用** |
| `note_use` | 刷新条目 last_used |
| `note_recall` | 只读冷存储条目 |
| `note_comment` | 挂 ephemeral TODO，置 dirty |
| `note_maintain` | 机械维护 + 返回 dirty / 超限清单 |
| `note_rewrite` | 唯一清 dirty 入口，`entries=[]` 可删组 |

`note_read` 双模式的设计意图是**默认省 context**：LLM 日常引用记忆只需要一个条目，不应该每次拉整组；`note_read_group` 只在维护整理时用，那时 LLM 需要看到所有邻居才能判断合并/去重/拆分。

## 隐式关联性

相似条目挤在同一组里，LLM 读一次就能看到"这个话题下的所有条目"，帮助判断合并/去重——这是记忆系统的一个隐式关键属性。如果条目变成没有邻居的孤岛，合并整理就退化成 N² 相似度匹配。

保留手段：

- **同 title 的写入自动落到同一组**——slug 由 title 派生，相同 title = 相同组。
- **维护工具一次拉整组**——让 LLM 看到邻居。
- **INDEX 用组作聚类锚**——让 LLM 从 title 就能判断哪个组放着相关内容（见下节）。

## INDEX —— 两级检索的第一级

INDEX 是这个记忆系统的**元问题解决方案**：

> **LLM 如何知道"记忆库里大概有些什么"？**

不解决这个问题，LLM 面对每个用户问题只有两个坏选择：**盲搜**（每次都调搜索，即使根本没有相关记忆）或**忘记搜索**（不知道记忆库里有相关内容，就当作没有）。

INDEX 是**始终注入到 system prompt** 的一份"目录页"：按分类列出所有活跃组的标题、条目数、dirty 状态，加上冷存储的概览。只有骨架，没有条目正文。

这就构成了两级检索：

1. **第一级 —— INDEX（常驻上下文）**：LLM 每一轮对话都能"看见"记忆库结构，无需调工具就知道"有没有相关主题"
2. **第二级 —— `note_read` / `note_search`（按需触发）**：只有当 INDEX 显示存在相关主题时，LLM 才拉具体内容

设计定位是**让 LLM 主动决策是否进一步检索**，而不是被动接受推送。INDEX 是记忆库的"目录页"，让 LLM 有能力做检索路由。

**实现形态**：INDEX 没有对应的磁盘文件，而是每一轮对话开始时由 provider 通过几条 SQL 查询实时构建，直接拼进 system prompt。每次读到的都是 SQLite 最新状态，不存在陈旧问题。

## 评论：ephemeral TODO，不是内容的一部分

评论（comment）是挂在条目上的临时标记，用来在对话中发现问题、在维护时处理问题。它有三个特点：

- **临时**：评论在重写时自动清空。它不应该成为条目内容的一部分。
- **触发 dirty**：加评论会标脏组，保证它不会在维护中被遗漏。
- **有类型**：`inaccurate` / `wrong` / `needs_improvement` / `conflicting` / `misplaced`——类型引导 LLM 在维护时采取对应的处理方式。

看到评论说"这个 API 应该用 foo.bar 而不是 foo.baz"，改的是**条目内容**里的 `foo.baz → foo.bar`，不是加一句"⚠️ Correction"。评论是元数据，不是内容。

## 响应式维护

维护是**响应式**的——响应用户/系统写入产生的脏标记。没有被标脏的组说明它没变化，不要擅自"改进"。

机械维护（`note_maintain`）检测到的信号有三类：
- **dirty 组**——有变化，需要 LLM 看一眼
- **超大组**——渲染超 50KB，需要 LLM 拆分
- **超限分类**——分类超 50 组，需要 LLM 合并/迁移

对于"过小组"或"过稀分类"，不主动检测——因为过小/过稀不是错误，只是可以更好。LLM 在处理脏组时顺手注意到可以合并，这才是正确的工作模式。主动扫描全库找碎片会产生大量低价值改动和噪音。

## Web 看板

用户可能想不导出 Markdown 就直接查看记忆库数据。provider 预留了只读连接（WAL 模式下读连接不与写入抢锁）的扩展点，可以挂到 dashboard 上展示：

- **Overview** — 分类 × 组数 × dirty 数
- **Dirty groups** — 待整理组列表 + 每个组的所有条目内容（保留隐式关联性 UX）
- **Group detail** — 单个组的所有条目 + last_used + 评论
- **FTS 搜索** — 直接调搜索索引
- **Cold browser** — 冷存储按批次时间倒序浏览
